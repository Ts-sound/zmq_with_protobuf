#include "poller.h"

bool zmq_with_protobuf::Poller::AddPub(const std::string& name,
                                       const std::string& topic,
                                       const std::string& endpoint) {
                                    
  return false;
}